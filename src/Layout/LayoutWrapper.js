import styled from "styled-components";

const LayoutWrapper = styled.section`
  .head-tag{
    h2{
      font-weight: bold;
      font-size: 40px;
      line-height: 120%;
      letter-spacing: 1px;
      color: #282A30;
    }
  }
    .container{
    }

  
`;

export default LayoutWrapper;
